customerHost: http://localhost:${mock.downstream.port}
clientHost: http://localhost:${mock.downstream.port}

mock:
  downstream:
    port: 8893
    
spring:  cloud:    consul:      config:        enabled: false