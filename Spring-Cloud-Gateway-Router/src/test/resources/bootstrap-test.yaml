customer:
  host: http://localhost:${mock.downstream.port}
  api-key: thebestsecret
client:
  host: http://localhost:${mock.downstream.port}
  api-key: thebestsecret

mock:
  downstream:
    port: 8893
    
spring:  cloud:    consul:      config:        enabled: false